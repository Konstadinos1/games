diff --git a/bellepros-poutine-catch/index.html b/bellepros-poutine-catch/index.html
index ef037f6..39c09f5 100644
--- a/bellepros-poutine-catch/index.html
+++ b/bellepros-poutine-catch/index.html
@@ -543,6 +543,7 @@
         const MISS_Y    = COUNTER_Y + 46;     // below this an item is "missed"
         const PLAY_MIN_X = 40, PLAY_MAX_X = DESIGN_W - 40;
         const TRAY_KEY_SPEED = 520;            // tray travel speed (px/s) while an arrow key is held
+        const TRAY_EDGE_MARGIN = 6;            // gap kept between the tray rim and the stage edge
 
         const C = {
             red:'#E8192C', redDk:'#7f1d1d', orange:'#FF6B35', amber:'#F59E0B', gold:'#FFD700',
@@ -600,6 +601,7 @@
                 t_miss:'RATÉ!', t_soggy:'DÉTREMPÉ!', t_perfect:'PARFAIT!', t_revived:'+3 VIES • C\'est reparti!', t_coupon_done:'Coupon réclamé ✓',
                 pu_magnet:'AIMANT!', pu_frenzy:'FRÉNÉSIE 2×!', pu_life:'+1 VIE!',
                 share_score:'Partager mon score', share_coupon:'Partager', link_copied:'Lien copié!',
+                sc_my_score:'Mon pointage', sc_beat:'Bats mon score!', sc_cta:'Joue gratis • gagne de la vraie bouffe', sc_saved:'Image enregistrée ✓',
                 share_text_score:"J'ai fait {n} points à Pluie de Poutine 🍟 Bats-moi et gagne de la bouffe gratuite chez Bellepros Laval 440, 5600 Robert-Bourassa!",
                 share_text_coupon:"J'ai gagné {offer} chez Bellepros Laval 440 🎉 Joue à Pluie de Poutine et gagne la tienne!",
                 streak_days:'Série', daily_bonus:'+1 vie aujourd\'hui!', daily_bonus_toast:'Bonus quotidien : +1 vie ❤',
@@ -638,6 +640,7 @@
                 t_miss:'MISS!', t_soggy:'SOGGY!', t_perfect:'PERFECT!', t_revived:'+3 LIVES • Back in!', t_coupon_done:'Coupon claimed ✓',
                 pu_magnet:'MAGNET!', pu_frenzy:'FRENZY 2×!', pu_life:'+1 LIFE!',
                 share_score:'Share my score', share_coupon:'Share', link_copied:'Link copied!',
+                sc_my_score:'My score', sc_beat:'Beat my score!', sc_cta:'Play free • win real food', sc_saved:'Image saved ✓',
                 share_text_score:'I scored {n} in Poutine Drop 🍟 Beat me and win free food at Bellepros Laval 440, 5600 Robert-Bourassa!',
                 share_text_coupon:'I won {offer} at Bellepros Laval 440 🎉 Play Poutine Drop and win yours!',
                 streak_days:'Streak', daily_bonus:'+1 life today!', daily_bonus_toast:'Daily bonus: +1 life ❤',
@@ -653,7 +656,8 @@
         const canvas = $('game'), ctx = canvas.getContext('2d');
         const screens = {
             menu:$('screen-menu'), pause:$('screen-pause'), coupon:$('screen-coupon'),
-            gameover:$('screen-gameover'), leaderboard:$('screen-leaderboard'), ad:$('screen-ad')
+            gameover:$('screen-gameover'), leaderboard:$('screen-leaderboard'), ad:$('screen-ad'),
+            owner:$('screen-owner')
         };
 
         // ============================================================================
@@ -1232,6 +1236,64 @@
             try { if (navigator.clipboard && navigator.clipboard.writeText){ navigator.clipboard.writeText((text+' '+url).trim()).then(()=>showToast(t('link_copied'))).catch(()=>showToast(t('link_copied'))); return; } } catch(e){}
             showToast(t('link_copied'));
         }
+        // ---- Shareable score card (branded 1080² image → Web Share, virality) ----
+        function buildScoreCard(g, W, H){
+            const cx=W/2;
+            g.fillStyle='#0b0b0f'; g.fillRect(0,0,W,H);
+            const rg=g.createRadialGradient(cx,H*0.40,40,cx,H*0.40,W*0.95);
+            rg.addColorStop(0,'rgba(232,25,44,.5)'); rg.addColorStop(.45,'rgba(245,158,11,.13)'); rg.addColorStop(1,'rgba(11,11,15,0)');
+            g.fillStyle=rg; g.fillRect(0,0,W,H);
+            g.fillStyle='#E8192C'; g.fillRect(0,0,W,18); g.fillRect(0,H-18,W,18);
+            g.fillStyle='#F59E0B'; g.fillRect(0,18,W,7); g.fillRect(0,H-25,W,7);
+            g.textAlign='center'; g.textBaseline='alphabetic';
+            // layout flows downward from the logo's actual bottom (logo aspect-safe)
+            let y;
+            if (logoImg && logoImg.complete && logoImg.naturalWidth>0){
+                const lw=W*0.42, lh=lw*logoImg.naturalHeight/logoImg.naturalWidth, ly=H*0.05;
+                g.drawImage(logoImg, cx-lw/2, ly, lw, lh);
+                y = ly + lh + H*0.05;
+            } else { g.fillStyle='#fff'; g.font='900 64px Fredoka One, Outfit, sans-serif'; g.fillText("BELLEPRO'S", cx, H*0.15); y=H*0.20; }
+            g.fillStyle='#FFD24D'; g.font='900 50px Fredoka One, Outfit, sans-serif';
+            g.fillText(t('game_name').toUpperCase(), cx, y); y += H*0.072;
+            g.fillStyle='rgba(255,255,255,.85)'; g.font='800 30px Outfit, sans-serif';
+            g.fillText(t('sc_my_score').toUpperCase(), cx, y); y += H*0.16;
+            g.save(); g.shadowColor='rgba(245,158,11,.55)'; g.shadowBlur=46;
+            g.fillStyle='#F59E0B'; g.font='900 200px Fredoka One, Outfit, sans-serif';
+            g.fillText(String(game.score), cx, y); g.restore(); y += H*0.075;
+            try { if (typeof Sprite!=='undefined' && Sprite.poutine) Sprite.poutine(g, cx, y, 128); } catch(e){} y += H*0.10;
+            g.fillStyle='#fff'; g.font='900 52px Fredoka One, Outfit, sans-serif';
+            g.fillText(t('sc_beat'), cx, y); y += H*0.035;
+            const cardH=H*0.135;
+            g.fillStyle='rgba(255,255,255,.07)'; roundRect(g, W*0.08, y, W*0.84, cardH, 26); g.fill();
+            g.fillStyle='#fff'; g.font='700 28px Outfit, sans-serif';
+            g.fillText(t('sc_cta'), cx, y + cardH*0.34);
+            g.fillStyle='#FFD24D'; g.font='800 25px Outfit, sans-serif';
+            g.fillText('Bellepros Laval 440 · 5600 Robert-Bourassa', cx, y + cardH*0.62);
+            g.fillStyle='rgba(255,255,255,.55)'; g.font='700 23px Outfit, sans-serif';
+            g.fillText('konstadinos1.github.io/games', cx, y + cardH*0.88);
+        }
+        function shareScoreCard(){
+            Audio.play('click');
+            const msg = t('share_text_score').replace('{n}', game.score);
+            let cv, g;
+            try { cv=document.createElement('canvas'); cv.width=1080; cv.height=1080; g=cv.getContext('2d'); } catch(e){}
+            if (!g || !cv.toBlob){ shareText(msg); return; }
+            try { buildScoreCard(g, 1080, 1080); } catch(e){ shareText(msg); return; }
+            cv.toBlob(function(blob){
+                if (!blob){ shareText(msg); return; }
+                try {
+                    const file = new File([blob], 'bellepros-pointage.png', { type:'image/png' });
+                    const url = (typeof location!=='undefined'&&location.href)?location.href:'';
+                    if (navigator.canShare && navigator.canShare({ files:[file] }) && navigator.share){
+                        navigator.share({ files:[file], text:msg, url, title:'Bellepros Laval 440' }).catch(()=>{});
+                        return;
+                    }
+                    const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='bellepros-pointage.png';
+                    document.body.appendChild(a); a.click(); a.remove();
+                    showToast(t('sc_saved')); shareText(msg);
+                } catch(e){ shareText(msg); }
+            }, 'image/png');
+        }
 
         function difficulty(){
             // ramps with elapsed time, eased
@@ -1350,10 +1412,11 @@
             if (game.spawnT<=0){ spawnItem(); game.spawnT = difficulty().spawn * (game.rush.active>0?0.5:1) * (0.8+Math.random()*0.5); }
             // held arrow keys steer the tray continuously (smooth desktop/accessibility control)
             const keyDir = (heldKeys.has('left')?-1:0) + (heldKeys.has('right')?1:0);
-            if (keyDir!==0) game.tray.target = keyTrayTarget(game.tray.target, keyDir, dt, DESIGN_W, TRAY_KEY_SPEED);
+            if (keyDir!==0) game.tray.target = keyTrayTarget(game.tray.target, keyDir, dt, game.tray.w, DESIGN_W, TRAY_KEY_SPEED);
             // tray follows target
+            const trayLim = trayLimits(game.tray.w, DESIGN_W);
             game.tray.x += (game.tray.target - game.tray.x) * Math.min(1, dt*20);
-            game.tray.x = clamp(game.tray.x, game.tray.w/2+6, DESIGN_W-game.tray.w/2-6);
+            game.tray.x = clamp(game.tray.x, trayLim.min, trayLim.max);
             // items
             for (let i=game.items.length-1;i>=0;i--){ const it=game.items[i];
                 it.y += it.vy*dt; it.rot += it.vr*dt; it.wob += dt*3;
@@ -1692,31 +1755,26 @@
         // ============================================================================
         //  GLOBAL LEADERBOARD — Supabase Edge Function (GET top 10 / POST signed submit)
         //  Reads + writes both go through the function, so the client needs no anon key.
-        //  Submissions are HMAC-signed; degrade silently if the backend is unreachable.
+        //  Submissions require a server-issued token (see SECURITY note in the Edge
+        //  Function). The previously embedded shared secret was removed on 2026-07-23
+        //  because it was trivially extractable from public source. Until a proper
+        //  per-session auth flow is implemented, submit() will return gracefully
+        //  without posting. The leaderboard GET (read) path still works.
         // ============================================================================
         const LB = {
             base:'https://apiexzijdmvtrmrzxgcp.supabase.co/functions/v1/leaderboard',
             anon:'__SUPABASE_ANON_KEY__',   // optional; only needed if the function gateway requires it
-            secret: atob('YnBfbHZsNDQwX3BjX3YxXzhLcTN3WjJ0UjduWDVtQjlwTDRjVjZ5RDFzSDBhSg=='),
             _headers(extra){ const h=Object.assign({}, extra||{}); if(!this.anon.startsWith('__')){ h['apikey']=this.anon; h['Authorization']='Bearer '+this.anon; } return h; },
-            async _hmac(msg){
-                const enc=new TextEncoder();
-                const key=await crypto.subtle.importKey('raw', enc.encode(this.secret), {name:'HMAC',hash:'SHA-256'}, false, ['sign']);
-                const mac=await crypto.subtle.sign('HMAC', key, enc.encode(msg));
-                return [...new Uint8Array(mac)].map(b=>b.toString(16).padStart(2,'0')).join('');
-            },
             async fetchTop(){
                 const r=await fetch(this.base,{method:'GET',headers:this._headers()});
                 if(!r.ok) throw new Error('http '+r.status);
                 const j=await r.json(); return Array.isArray(j.top)?j.top:[];
             },
             async submit(name, score){
-                const ts=Date.now(), nonce=Math.random().toString(36).slice(2,10);
-                const sig=await this._hmac(name+'|'+score+'|'+ts+'|'+nonce);
-                const r=await fetch(this.base,{method:'POST',headers:this._headers({'content-type':'application/json'}),body:JSON.stringify({name,score,ts,nonce,sig})});
-                const j=await r.json().catch(()=>({}));
-                if(!r.ok) throw new Error((j&&j.error)||('http '+r.status));
-                return j;
+                // SECURITY: client-side signing removed. Returns a no-op result so the
+                // game does not crash; the score is not submitted until server-issued
+                // tokens are wired up. See leaderboard Edge Function for the plan.
+                return { ok:false, skipped:'client_signing_disabled' };
             }
         };
         const _esc = s => String(s).replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
@@ -1972,13 +2030,27 @@
         // Held-key state so the tray glides smoothly while a key is down (see updatePlay),
         // instead of stepping once per OS key-repeat event.
         const heldKeys = new Set();
+        // While the player is typing (leaderboard nickname, owner ad URLs) the keyboard belongs to the
+        // field, not the game — otherwise arrows lose the caret and Space/Enter act on the run behind.
+        function isTypingTarget(el){
+            if (!el) return false;
+            const tag = el.tagName;
+            return tag==='INPUT' || tag==='TEXTAREA' || el.isContentEditable===true;
+        }
+        // An overlay that sits above the canvas (leaderboard, owner panel) owns the keyboard too.
+        function modalIsOpen(){
+            return ['leaderboard','owner'].some(k => screens[k] && !screens[k].classList.contains('hidden'));
+        }
         document.addEventListener('keydown',(e)=>{
+            if (isTypingTarget(e.target) || modalIsOpen()) return;
             if (game.state==='playing'){
                 if (e.key==='ArrowLeft'){ heldKeys.add('left'); e.preventDefault(); }
                 else if (e.key==='ArrowRight'){ heldKeys.add('right'); e.preventDefault(); }
-                else if (e.key===' '||e.key==='p'||e.key==='P') pauseGame();
-            } else if (game.state==='paused' && (e.key===' '||e.key==='p'||e.key==='P')) resumeGame();
-            else if (game.state==='menu' && e.key==='Enter') startGame();
+                // preventDefault stops Space from re-activating a focused HUD button on keyup,
+                // which would instantly undo the pause/resume we just performed.
+                else if (e.key===' '||e.key==='p'||e.key==='P'){ e.preventDefault(); pauseGame(); }
+            } else if (game.state==='paused' && (e.key===' '||e.key==='p'||e.key==='P')){ e.preventDefault(); resumeGame(); }
+            else if (game.state==='menu' && e.key==='Enter'){ e.preventDefault(); startGame(); }
         });
         document.addEventListener('keyup',(e)=>{
             if (e.key==='ArrowLeft') heldKeys.delete('left');
@@ -2010,7 +2082,7 @@
         $('btnCouponContinue').addEventListener('click', continueAfterCoupon);
         $('btnRevive').addEventListener('click', startAd);
         // share (invite friends → more players → more traffic)
-        $('btnShareScore').addEventListener('click', ()=>{ Audio.play('click'); shareText(t('share_text_score').replace('{n}', game.score)); });
+        $('btnShareScore').addEventListener('click', shareScoreCard);
         $('btnShareCoupon').addEventListener('click', ()=>{ Audio.play('click'); const tier=game.couponTier; const offer = tier ? (lang==='fr'?tier.fr:tier.en).title : ''; shareText(t('share_text_coupon').replace('{offer}', offer)); });
         // simulated staff redemption (anti-reuse)
         $('btnStaffRedeem').addEventListener('click', ()=>{ markRedeemed(game.couponCode); $('couponStamp').classList.remove('hidden'); $('btnStaffRedeem').classList.add('hidden'); Audio.play('tick'); showToast(t('already_redeemed')); });
@@ -2029,9 +2101,20 @@
         //  HELPERS + RESPONSIVE SCALING
         // ============================================================================
         function clamp(v,a,b){ return v<a?a:v>b?b:v; }
+        // Leftmost/rightmost tray centre the stage allows, given the tray's width.
+        function trayLimits(trayW, width){
+            const half = trayW/2 + TRAY_EDGE_MARGIN;
+            return { min: half, max: width - half };
+        }
         // Pure helper (unit-tested in tests/controls.test.js): tray target after dt seconds of held-key
-        // steering. dir is -1 (left), 0 (none) or 1 (right); result stays within [0, width].
-        function keyTrayTarget(target, dir, dt, width, speed){ return clamp(target + dir*speed*dt, 0, width); }
+        // steering. dir is -1 (left), 0 (none) or 1 (right).
+        // Clamping to the REACHABLE range (not 0..width) matters: the rendered tray is limited to
+        // [half, width-half], so letting the target run past that would bank up invisible overshoot
+        // and the tray would ignore the opposite key until it unwound.
+        function keyTrayTarget(target, dir, dt, trayW, width, speed){
+            const lim = trayLimits(trayW, width);
+            return clamp(target + dir*speed*dt, lim.min, lim.max);
+        }
         function vibrate(p){ try{ if (navigator.vibrate) navigator.vibrate(p); }catch(e){} }
 
         function setupCanvas(){
